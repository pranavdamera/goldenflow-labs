[build]
  command = "npm run build"
  publish = "out"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    X-Frame-Options = "SAMEORIGIN"

# Optional redirect (uncomment and configure as needed)
# [[redirects]]
#   from = "https://goldenflowlabs.com/*"
#   to = "https://www.goldenflowlabs.com/:splat"
#   status = 301
#   force = true